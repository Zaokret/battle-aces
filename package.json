{
  "dependencies": {
    "jimp": "^0.22.12",
    "json2md": "^2.0.1"
  },
  "name": "battle-aces",
  "description": "Scraper for https://www.playbattleaces.com/",
  "version": "1.0.0",
  "main": "",
  "scripts": {
    "scrape": "node units.js && node download-images.js && node png-to-jpg.js && node json-to-md.js",
    "lint": "prettier ./data/units.json --write",
    "server:dev": "npm run dev --prefix server",
    "server:prod": "npm run prod --prefix server"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "prettier": "^3.3.2"
  }
}
